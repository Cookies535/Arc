# Domain Docs Configuration

**Layout:** Single-context

- **Global Context:** Located at `CONTEXT.md` in the repository root.
- **Architectural Decision Records (ADRs):** Located in the `docs/adr/` directory.

Skills should read `CONTEXT.md` to learn the project's domain language and review `docs/adr/` for past architectural decisions.
